{
  "schema_version": 2,
  "target_nx_version": "NX 2606",
  "runtime_mode": "manual_nx",
  "mcp_role": "api_review_only",
  "requires_prepared_nx_environment": true,
  "requires_manual_user_nx_ui_run": true,
  "agent_execution": false,
  "allow_launch_or_close_nx": false,
  "allow_existing_work_part": false,
  "allow_overwrite": false,
  "max_repair_attempts": 3,
  "lookup_tools": [
    "dc_lookup_pattern",
    "dc_search",
    "dc_semantic_search",
    "dc_get_api_info",
    "dc_list_namespace"
  ],
  "forbidden_execution_tools": [
    "dc_run_snippet",
    "dc_run_journal"
  ],
  "observed_runtime_boundary": {
    "evidence": "integration_002 probe 01",
    "result": "dc_run_journal resolved run_journal.exe, timed out after 90 seconds, and did not use the already-open NX UI",
    "consequence": "Agent execution is unsupported; the user runs every Journal manually from NX."
  },
  "preflight": [
    {
      "tool": "dc_lookup_pattern",
      "arguments": {"query": "create new part", "limit": 3}
    },
    {
      "tool": "dc_get_api_info",
      "arguments": {"info_type": "method", "class_name": "NXOpen.PartCollection", "method_name": "NewDisplay"}
    }
  ],
  "journal_preparation": {
    "command": "skills/nx-cad/scripts/prepare-dc-mcp-journal <canonical-probe> <workspace-copy> --review-evidence <review.json> --manual-user-run",
    "windows_command": "py -3 skills\\nx-cad\\scripts\\prepare-dc-mcp-journal <canonical-probe> <workspace-copy> --review-evidence <review.json> --manual-user-run",
    "scope": "workspace copy only; the preparation command refuses canonical skill outputs and existing files",
    "shared_support": "multiple probes may share one execution directory; an existing _probe_support.py is reused only when byte-identical and is never overwritten",
    "execution_policy_overrides": {
      "mode": "manual_nx",
      "manual_user_run_required": true,
      "agent_execution": false
    },
    "review_marker": "replace STATIC_ONLY_NXOPEN_REVIEW in the workspace copy with MCP_API_REVIEW containing only lookup tools and facts actually checked",
    "required_check": "py -3 skills/nx-cad/scripts/check-journal <journal> --strict-geometry"
  },
  "runs": [
    {
      "sequence": 1,
      "journal": "nx2606/01_create_part.py",
      "expected_probe": "01_create_part",
      "expected_body_count": 0,
      "execution": "user selects and runs the Journal manually in the NX UI",
      "required_outputs": ["prt", "nxreport"]
    },
    {
      "sequence": 2,
      "journal": "nx2606/06_sweep_two_sections.py",
      "expected_probe": "06_sweep_two_sections",
      "expected_body_count": 1,
      "execution": "user selects and runs the Journal manually in the NX UI",
      "required_outputs": ["prt", "nxreport"]
    }
  ],
  "deferred_initial_runs": [
    "nx2606/07_sweep_angular_law.py",
    "nx2606/10_step_ap242.py"
  ],
  "acceptance": [
    "the user reports running the exact workspace Journal manually in NX",
    "the Journal reports schema v2 execution provenance user:nx_ui",
    "a unique nxreport exists",
    "actual body count equals expected body count",
    "all critical features are true",
    "a unique non-empty PRT exists",
    "the prior run artifacts remain unchanged"
  ]
}
